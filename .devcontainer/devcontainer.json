{
  "name": "Codespace to perform Kubernetes Deep Dive for Developers Labs",
  "remoteEnv": {
    "INSTALLATION_TYPE": "labs"
  },
  "hostRequirements": {
    "cpus": 4,
    "memory": "8gb",
    "storage": "32gb"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "files.autoSave": "onFocusChange",
        "editor.tabSize": 2
      },
      "workbench.editorAssociations": {
          "*.md": "vscode.markdown.preview.editor"
      },
      "extensions": [
        "ms-azuretools.vscode-docker"
      ]
    },
    "codespaces": {
      "openFiles": [
        "codespace-labs.md"
      ]
    }
  },
  "postCreateCommand": "(minikube start && /workspaces/k8s-dev-v2/monitoring/setup-monitoring.sh) || echo 'Could not auto-configure. Skipping.' "
}
